using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.Collections.Generic;

namespace BingVoiceSystem.Tests
{
    /*[TestClass]
    public class UserTest
    {
        [TestMethod]
        public void UsernamePassword_Correct_True()
        {
            User u = new User(1, "Jeff Bridges", "Lebowski", new List<Role>() { Role.Registered });
            Assert.IsTrue(u.CheckUserNameAndPassword("Jeff Bridges", "Lebowski"));
        }

        [TestMethod]
        public void UsernamePassword_InCorrect_True()
        {
            User u = new User(1, "Jeff Bridges", "Lebowski", new List<Role>() { Role.Registered });
            Assert.IsFalse(u.CheckUserNameAndPassword("Jeff Bridges", "lebowski"));
        }

        [TestMethod]
        public void ChangePassword_MatchingPassword_True()
        {
            User u = new User(1, "Jeff Bridges", "OldPass", new List<Role>() { Role.Registered });
            u.ChangePassword("OldPass", "NewPass");
            Assert.IsTrue(u.CheckUserNameAndPassword("Jeff Bridges", "NewPass"));
            Assert.IsFalse(u.CheckUserNameAndPassword("Jeff Bridges", "OldPass"));
        }
    }*/
}
